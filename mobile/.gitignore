node_modules/

# Generated by `cap sync` from capacitor.config.ts. It embeds SATE_URL — the deployment's
# instance URL — so it must never be committed to this public repo. Regenerate with:
#   SATE_URL=https://sate.example.com npm run sync
ios/App/App/capacitor.config.json

# Web assets copied into the native project by `cap sync`.
ios/App/App/public/

# Xcode / SwiftPM build output
ios/App/build/
ios/App/DerivedData/
ios/App/Pods/
ios/App/.swiftpm/
*.xcuserdatad
xcuserdata/
